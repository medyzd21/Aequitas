"""Reflex project configuration for the Aequitas frontend.

Run from this directory:

    pip install reflex
    reflex init      # first time only
    reflex run
"""
import reflex as rx

config = rx.Config(
    app_name="aequitas_rx",
    tailwind=None,
)
